# tynice.com

Wedding memory site for Tyson & Janice. Static Cloudflare Pages — one HTML file.

## Dev

```bash
just dev      # serve public/ locally
just deploy   # push to Cloudflare Pages
```

Auto-deploys on push to `main`. Needs `CLOUDFLARE_API_TOKEN` in GitHub Actions secrets.
